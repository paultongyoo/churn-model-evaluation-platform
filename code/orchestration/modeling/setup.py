"""
Setup script for the modeling package in the MLOps churn pipeline project.
"""

from setuptools import find_packages
from setuptools import setup

setup(
    name="modeling",
    version="0.1.0",
    packages=find_packages(),
)
